import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "Product Strategy Coach | Lenny's Podcast Insights",
  description: "AI-powered product strategy coaching based on insights from world-class product leaders featured on Lenny's Podcast.",
  keywords: ["product strategy", "product management", "coaching", "Lenny's Podcast"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.variable} font-sans antialiased`}>
        {children}
      </body>
    </html>
  );
}
